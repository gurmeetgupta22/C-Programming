#include<stdio.h>
#include<stdlib.h>
int main(){
    int n,;
    printf("Enter Array size: ");
    scanf("%d", &n);
    int arr[n];
    printf("Enter Array Elements: ");
    for(int i=0; i<n; i++){
        scanf("%d", &arr[i]);
    }
    for(int i=1;i<n;i++){
        int curr = arr[i];
        
    }
}